# HOOKX local environment template.
# Copy to .env for machine-local values. Never commit .env.
#
# This foundation does not load provider credentials or a database.
# Persistence, webhook ingestion, and Razorpay integration are not implemented yet.

HOOKX_API_HOST=127.0.0.1
HOOKX_API_PORT=8787
